# Frontend — base URL of the FastAPI backend
# Local development: http://127.0.0.1:8000
# Production build: https://my-web-app-cyyv.onrender.com  (set in .env.production)
VITE_API_URL=http://127.0.0.1:8000

# Backend config lives in backend/.env — see backend/.env.example for all options.
