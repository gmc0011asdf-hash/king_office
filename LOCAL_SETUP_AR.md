# تشغيل نظام مكتب الملك محلياً بالكامل

أصبح النظام يستخدم قاعدة بيانات **SQLite محلية** ولا يحتاج إلى Supabase أو PostgreSQL أو Render.

## التشغيل على Windows

1. ثبّت Python 3.11 أو أحدث.
2. ثبّت Node.js 20 أو أحدث.
3. شغّل الملف `START_LOCAL.bat`.
4. افتح النظام على `http://127.0.0.1:5173`.

في أول تشغيل تُنشأ قاعدة جديدة وفارغة في:

`backend/data/king_office.sqlite3`

لا تُضاف أي بيانات تجريبية أو بيانات عملاء أو مشتركين. يُنشأ حساب مدير واحد فقط. عند ترك كلمة مرور المدير فارغة في الإعدادات، توجد بيانات الدخول الأولية في:

`backend/logs/FIRST_LOGIN_ADMIN_PASSWORD.txt`

احذف الملف بعد تسجيل الدخول وتغيير كلمة المرور.

## بدء النظام من الصفر لاحقاً

أوقف الخادم ثم نفّذ:

```bat
python scripts\clean_db.py --yes
```

بعد ذلك شغّل `START_LOCAL.bat` مجدداً. ستنشأ قاعدة فارغة جديدة.

## النسخ الاحتياطي

النسخ المحلية تحفظ افتراضياً في:

`backend/backups`

امتداد النسخة هو `.sqlite3` ويمكن إنشاؤها أو استرجاعها من إعدادات النظام.

## ملاحظات الخصوصية

- ملف قاعدة البيانات غير مرفوع إلى GitHub بسبب إعدادات `.gitignore`.
- لا توجد وصلة افتراضية إلى Supabase أو Render.
- Sentry وResend والخدمات الاختيارية معطلة افتراضياً.
- مزامنة FTTH أو Telegram لا تعمل إلا عند إعدادها يدوياً، بينما تبقى جميع بيانات النظام نفسها محلية.
